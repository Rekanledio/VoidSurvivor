# Void Survivor — Tasks

## Completed: M0 — Project Documentation Initialization (2026-08-14)

### M0.1 Documentation
- [x] Define project scope
- [x] Define MVP boundaries
- [x] Define long-term context strategy
- [x] Add all project documents to the real repository
- [x] Review documents after first Unity project creation

### M0.2 Tooling
- [x] Verify WorkBuddy project workflow
- [x] Verify Unity MCP connection for this project
- [x] Verify Git workflow
- [x] Verify Unity 6.3 LTS version

## Completed: M1 — Unity Project Initialization (2026-08-14)
- [x] Create Unity project (created manually by user, Unity 6000.3.21f1)
- [x] Initialize Git repository (main branch, Unity .gitignore)
- [x] Create folder structure (per ARCHITECTURE.md)
- [x] Create initial scene (Assets/Scenes/SC_Main.unity, orthographic camera)
- [x] Configure Input System (activeInputHandler=1, Input System only)
- [x] Configure basic project settings (2D default behavior mode, URP, Linear color space)
- [x] Connect Unity MCP (CoplayDev unity-mcp, verified via WorkBuddy)
- [x] Verify WorkBuddy can read and modify project (scene created and modified via MCP)
- [x] Run first successful build/test (play mode test passed, no console errors)

## Completed: M2 — Core Framework (2026-08-14)
- [x] GameState transition API on GameManager (TryChangeState, legal-transition table, GameStateChanged broadcast)
- [x] Core event bus for cross-system communication (EventBus: Subscribe/Unsubscribe/Publish/Clear, struct events)
- [x] Scene flow entry for MainMenu / Gameplay / Result (SceneFlow + SceneIds, same-scene reload guard)
- [x] Lifecycle documented (GameBootstrap → GameManager.Awake → scene Awake/Start; EventBus lazy static)
- [x] No gameplay systems introduced (Player/Enemy/Weapon/Wave/Shop untouched)
- [x] Verified: in-play smoke test 33 checks / 0 failures; GameManager, EventBus, SceneFlow covered; temp test code removed

## Completed: M3 — Player System (2026-08-14)
- [x] Player movement via Input System (reused existing InputSystem_Actions Player/Move, WASD + arrows + gamepad)
- [x] 8-way movement with consistent diagonal speed (input normalized, magnitude capped at 1)
- [x] Rigidbody2D.MovePosition physics-based movement (no Transform hack), configurable MoveSpeed
- [x] Configurable arena bounds clamp (placeholder, no real map)
- [x] PlayerStats with all 10 MVP stats (GAME_DESIGN.md), plain base values + read accessors
- [x] PlayerHealth: TakeDamage (flat armor reduction) / Heal / FullHeal / IsDead, HP clamped to [0, MaxHP]
- [x] Death: single trigger, IsDead flag, PlayerDied event via EventBus (no Game Over flow)
- [x] CameraFollow: smooth exponential orthographic follow (no Cinemachine), offset, optional bounds
- [x] Player prefab + placeholder sprite placed in SC_Main
- [x] Verified: in-play smoke test 29 checks / 0 failures; dynamic play checks (camera convergence, bounds, component state); temp test code removed
- [x] Bug fix (2026-08-14): Rigidbody2D interpolation = Interpolate (removes movement jitter); Camera orthographicSize 5 → 8 (viewport matches arena); PlayerController input switched to serializable InputActionAsset reference (fixes non-persisted InputActionReference). Verified: full -20..+20 traversal, bounds hold, no pull-back, camera follows, 0 console errors.

## Current Milestone: M4 — Enemy System
- [x] M4.1 Enemy base framework (2026-08-14): EnemyData (ScriptableObject static config), EnemyStats (read-only runtime view), EnemyHealth (CurrentHP/TakeDamage/clamp/single death + EnemyDied event), EnemyController (common refs + Player target), EnemyBase prefab + EnemyPlaceholder sprite + EnemyBase.asset. Verified: 27/27 probe checks PASS, 0 errors/warnings.
- [x] M4.2 Chaser AI (2026-08-14): ChaserAI pursues the player at EnemyData MoveSpeed via Rigidbody2D.MovePosition, reuses EnemyController references (no per-frame GetComponent/Find), stops when dead. ChaserData.asset (moveSpeed 3.5) + Chaser.prefab (base + ChaserAI). Verified: 15/15 probe checks PASS (instantiation, HP, target resolve, speed 3.5 exact, approach, re-target, continuous chase, death stop, player intact), 0 errors/warnings.
- [x] M4.3 Runner AI (2026-08-14): RunnerAI — faster pursuer (RunnerData moveSpeed 6 vs Chaser 3.5), same pursuit pattern as ChaserAI (MovePosition, EnemyController refs, stops when dead). RunnerData.asset + Runner.prefab (base + RunnerAI). Verified: 20/20 probe checks PASS (speed 6.00 exact over fixed physics frames, re-target, chase, physics hold near player, death stop 0.000, Chaser unaffected, player intact), 0 errors/warnings.
- [x] M4.4 Shooter AI (2026-08-14): ShooterAI — approaches only outside AttackRange, stops inside it, fires a minimal Projectile at the player when in range and off cooldown (Time.time-based). Minimal M4.4 Projectile (kinematic, velocity, lifetime, contact damage via PlayerHealth.TakeDamage — flagged as temporary until M5). ShooterData.asset (speed 2.5 / range 6 / cooldown 1.5 / dmg 8 / HP 25) + ShooterProjectile.prefab + Shooter.prefab. Verified: 27/27 probe checks PASS (approach/stops at range edge, fire→hit→damage via player HP, cooldown window, fires after cooldown, out-of-range silence, death stops move+attack, Chaser/Runner regression, no projectile residue), 0 errors/warnings.
- [x] M4.5 Tank AI (2026-08-14): TankAI — slow, high-HP pursuer (same pursuit pattern as ChaserAI; MovePosition + EnemyController refs; stops when dead; no special attack). TankData.asset (moveSpeed 2 < Chaser 3.5, maxHP 120 > others 30) + Tank.prefab (base + TankAI). Verified: 23/23 probe checks PASS (speed 2.00 exact over fixed physics frames, re-target on player move/crossing, TakeDamage→90, clamp 0, single EnemyDied, death stop, Chaser/Runner/Shooter regression), 0 errors/warnings.
- [x] M4.6 Minimal spawn entry (2026-08-14): EnemySpawner — Start-time single spawn of one instance per configured prefab at fixed cardinal offsets (10 units) around the player; no wave/timer/loop logic (M8 owns waves). EnemySpawner object in SC_Main wired to Chaser/Runner/Shooter/Tank prefabs. Verified: 22/22 probe checks PASS (4 enemies spawned at ~10, count == 4, components + target resolved, Chaser/Runner/Tank AI moving, no duplicate spawning) + manual observation of all 4 enemies in-play (Chaser/Runner/Tank pursue, Shooter fires projectiles), 0 errors/warnings.

## M4 — Enemy System: COMPLETE (2026-08-14)

## M5 — Combat System: COMPLETE (2026-08-14)
- [x] M5.1 Combat Base Framework (2026-08-14): IDamageable (PlayerHealth/EnemyHealth implement), DamageRequest/DamageResult structs, CombatSystem static unified ApplyDamage entry, DamageApplied event; Projectile migrated off direct PlayerHealth coupling to the combat entry (with source). Verified: 23/23 probe checks PASS, 0 errors/warnings.
- [x] M5.2 Enemy Death & Kill Attribution (2026-08-14): EnemyKilled event (Enemy/Killer) published once per lethal hit on EnemyHealth with a valid Source; EnemyController destroys dead enemies (pool in M7). Verified: 28/28 probe checks PASS, 0 errors/warnings.
- [x] M5.3 Pickup System (2026-08-14): PlayerProgress (XP/Gold) + PickupType/PickupData + Pickup collect + PickupSystem (EnemyKilled → drop XP+Gold at death position). Verified: 25/25 probe checks PASS + manual play, 0 errors/warnings.
- [x] M5.4 Player Attack Path (2026-08-14): PlayerAttack — Attack(target) routes a DamageRequest (Source = Player, PlayerStats.Damage) through CombatSystem; no auto-attack/weapon logic (M6 owns weapons). Verified: 23/23 probe checks PASS, 0 errors/warnings.

## M6 — Weapon System: COMPLETE (2026-08-16)
- [x] M6.1 Weapon Base Framework (2026-08-16): WeaponData (SO: name/damage/cooldown/range), WeaponController (runtime; Attack(target) → PlayerAttack, lazy parent resolve), WeaponSlot (container), WeaponManager (4 slots, Equip/Unequip/Get with bounds checks). WeaponBaseData.asset + WeaponBase.prefab (test base, not one of the 4 formal weapons); Player prefab gains WeaponManager. Verified: 32/32 probe checks PASS, 0 errors/warnings. [M6 COMPLETE]
- [x] M6.2 Pulse Gun (2026-08-16): PulseGun (auto-attack loop via Time.time cooldown, minimal targeting = nearest live EnemyHealth within Range, fires one PulseProjectile with Source = player; self-equips into slot 0), PulseProjectile (straight flight, lifetime, contact → CombatSystem, skips own source), PulseGunData (dmg 5 / cd 0.25 / range 8 — high fire rate, single target), PulseGun.prefab + PulseProjectile.prefab; PlayerAttack gains Attack(target, damage) overload (old API regression-compatible); WeaponController exposes Owner + protected damage attack. SC_Main: PulseGun under player. Verified: 27/27 probe checks PASS + manual play (auto-combat), 0 errors/warnings.
- [x] M6.3 Scatter Blaster (2026-08-16): ScatterBlasterData : WeaponData (+ projectileCount 5 / spreadAngle 45), ScatterBlaster (auto-attack loop, nearest-target for the fan center, fires N PulseProjectiles simultaneously in a deterministic uniform fan), reuses PulseProjectile. ScatterBlasterData (dmg 3 / cd 0.8 / range 7) + prefab; WeaponController.Owner lazy-resolve fix. Verified: 42/42 probe checks PASS + manual play, 0 errors/warnings.
- [x] M6.4 Boomerang (2026-08-16): BoomerangData : WeaponData (+ maxDistance 6 / outSpeed 8 / returnSpeed 10), Boomerang single-flight auto-throw, BoomerangProjectile two-phase (out to maxDistance, return re-aims at player's current position, hit-once per enemy). BoomerangData (dmg 7 / cd 1.2 / range 8) + prefabs. Verified: 37/37 probe checks PASS, 0 errors/warnings.
- [x] M6.5 Arc Blade (2026-08-16): close-range area weapon — ArcBladeData : WeaponData (reuses base fields; Range = attack radius), ArcBlade (auto-attack loop; each strike does ONE OverlapCircleAll centered on the player and hits every live EnemyHealth inside Range exactly once — deduped, dead skipped, self ignored; each hit via PlayerAttack → CombatSystem). ArcBladeData (dmg 8 / cd 0.9 / range 2.5 — close range, low-mid rate, high single-target presence) + ArcBlade.prefab (no projectile). Verified: 29/29 probe checks PASS (data, slot-0 equip, first strike hits near + edge once each (30→22), out-of-range untouched, player never hit, per-target once per strike (30→14 after 2 strikes), strike cooldown ≈ 0.9 (0.900s), kill → EnemyKilled Killer == Player, dead enemy destroyed, out-of-range still untouched after kill, pickups, Pulse/Scatter/Boomerang/PlayerAttack/PlayerHealth/PlayerProgress/Spawner regressions), 0 errors/warnings.

## M7 — Object Pool: COMPLETE (2026-08-16)
- [x] M7.1 Object Pool Base Framework (2026-08-16): `IPoolable` (OnSpawn/OnDespawn) + generic `ObjectPool<T>` in Core (namespace VoidSurvivor.Core). Pre-warms instances; Get() activates + OnSpawn, Release() OnDespawn + deactivates; grows on demand when exhausted (never rejects); double-release guarded via in-pool set; Clear() destroys all managed objects. SetActive lifecycle control; no Update/FixedUpdate allocations; standalone — NOT wired into Enemy/Pickup/Projectile lifecycles yet (M7.2). Verified: 19/19 probe checks PASS (capacity 3, Get activates + OnSpawn once, Release deactivates + OnDespawn, Get→Release→Get reuses same instance, double-release ignored + no double OnDespawn, exhaustion grows total 3→4, Clear empties pool + scene 4→0 no residue), 0 errors/warnings.
- [x] M7.2.1 Projectile Pool Integration (2026-08-16): PulseProjectile / BoomerangProjectile / Shooter Projectile moved from Instantiate/Destroy to the M7.1 ObjectPool. Static per-type shared pool (Pulse Gun + Scatter Blaster reuse one PulseProjectile pool), lazy EnsurePool(prefab), static Spawn(...) = Get + position + Init, lifetime/hit despawn → Release. IPoolable OnDespawn clears all runtime state (velocity/source/damage/timer, boomerang phase/origin/hit-set) so recycling starts clean; Init stays per-shot injection. Boomerang ActiveCount now maintained at Spawn (+1) / DespawnSelf (−1) instead of OnDestroy (pool warmup also runs Release/OnDespawn). Per-type static pool reset on play start (SubsystemRegistration). Verified: 38/38 probe checks PASS (shared pool, reuse same instance, state reset, hit → Release + damage, lifetime → Release, released never moves, shooter source + damage, boomerang ActiveCount 0→2→0, hit-once per throw + hit-set reset HP 23→16, return tracks moved player, Pulse Gun auto-attack kill → Killer == Player, pickups, regressions), 0 errors/warnings.
- [x] M7.2.2 Enemy Pool Integration (2026-08-16): EnemyController implements IPoolable (static Spawn(pool, position); OnSpawn resets health + notifies child AI via GetComponents<IPoolable>; OnDespawn notifies AI + zeroes velocity; death → DespawnSelf → Release instead of Destroy; EventBus subscribe stays in Awake — pool reuse never re-subscribes). EnemyHealth.ResetForSpawn (full HP, clear IsDead). EnemySpawner owns per-prefab lazy ObjectPools (Dictionary<prefab, ObjectPool<EnemyController>>, capacity 1, parent = spawner) and spawns via EnemyController.Spawn — same offsets/count as M4.6. ShooterAI implements IPoolable (OnSpawn resets _nextAttackTime — no stale cooldown). Chaser/Runner/Tank AI need no per-state reset (stateless, stopped by inactive + IsDead check). Verified: 31/31 probe checks PASS (pool warm/activate/reuse/deactivate, HP+IsDead reset across lives, EnemyDied exactly once per life, no duplicate subscribe/handling, Chaser re-tracks after respawn, released enemy never moves, Shooter fires fresh + after respawn + stops when dead, double-release safe, Clear no residue, PickupSystem/EnemyKilled/PlayerHealth/PlayerProgress/weapon prefabs regressions), 0 errors/warnings.
- [x] M7.2.3 Pickup Pool Integration (2026-08-16): Pickup implements IPoolable — static Spawn(pool, position) = Get + _myPool + reposition; collection → DespawnSelf → Release (no Destroy on the normal collect path). OnSpawn/OnDespawn are no-ops (Pickup has NO runtime state — PickupData is static per prefab; being inactive stops the trigger). PickupSystem owns one lazy ObjectPool per prefab (XP pool + Gold pool, capacity 16, parent = system) and spawns via Pickup.Spawn — EnemyKilled still yields exactly 1 XP + 1 Gold at the enemy's ACTUAL death position. Collection-once semantics preserved (inactive after release; re-spawn can collect again). Verified: 34/34 probe checks PASS (pools warm/activate/reuse/deactivate/double-release-safe/counts, XP 10 + Gold 5 amounts, collect +10/+5 exactly once + one PickupCollected + never settles after release, recycle collect→release→collect, multiple pickups coexist, EnemyKilled spawns 1 XP + 1 Gold at death position, second kill no state bleed, regressions), 0 errors/warnings. M7.2 complete (Projectile + Enemy + Pickup all pooled).

## M8 — Wave System: COMPLETE (2026-08-16) — final regression 58/58 PASS, 3 clean play/stops
- [x] M8.1 Wave Lifecycle & Spawn Scheduling (2026-08-16): `WaveManager` (Enemy namespace, attached to EnemySpawner in SC_Main) — waves 1..10, Time.deltaTime-accumulated wave time that only advances while Playing (Paused/LevelUp/Shop freeze, GameOver/Victory stop, fresh run on re-Playing), spawn scheduling via `EnemySpawner.SpawnEnemy(prefab, pos)` (per-wave config: duration/enemyCount/spawnInterval in a centralized table; deterministic type rotation `index % prefabs.Count`; M4.6 cardinal spawn positions; Wave 10 currently spawns normal enemies — boss reserved for M8.3). Publishes `WaveStarted`/`WaveCompleted` (added to GameEvents). Wave completion = duration + schedule (NOT enemy-alive count); no wave 11 after wave 10 (idle, no Victory logic). `EnemySpawner`: removed Start-time auto spawn; kept pools/GetPool/SpawnEnemy public entry + GetEnemyPrefab + GetSpawnPosition. WaveManager never touches ObjectPool directly (Wave → EnemySpawner → Pool). Placeholder tuning (W1 8s/5/1.6s → W10 12s/15/0.7s) documented as non-final. Verified: 42/42 probe checks PASS (WaveStarted(1) once, CurrentWave progression 1→2, spawn count 5 + interval + stop-at-count, WaveCompleted(1) once, Paused/LevelUp/Shop freeze wave time + resume without re-publishing WaveStarted, GameOver stops + MainMenu→Playing fresh run at wave 1, StartWave(10) → WaveCompleted(10) → no WaveStarted(11) → idle, 4-type rotation, pool-based spawns, AI/weapons/pickups regressions), 0 errors/warnings.
- [x] M8.2 Wave Difficulty Growth (2026-08-16): runtime per-wave multiplier — `EnemyStats.WaveMultiplier` (plain non-serialized field, default 1; MaxHP/Damage/MoveSpeed × multiplier, AttackRange/AttackCooldown unchanged). Injection chain: WaveManager (per-wave multiplier in WaveConfig/WaveTable: W1 1.00 → W10 1.45, M8.2 base slope) → EnemySpawner.SpawnEnemy(prefab, pos, multiplier) → EnemyController.Spawn(pool, pos, multiplier) sets stats.WaveMultiplier THEN EnemyHealth.ResetForSpawn (CurrentHP uses scaled MaxHP); OnDespawn resets multiplier to 1 so pool reuse never leaks difficulty. EnemyData assets untouched (verified via serialized values). duration/enemyCount/spawnInterval unchanged from M8.1. Verified: 39/39 probe checks PASS (W1/W5/W10 multipliers 1.00/1.20/1.45, Chaser/Runner/Shooter/Tank MaxHP + MoveSpeed + Shooter Damage scaled, Range/Cooldown unchanged, spawn injection order, TakeDamage on scaled HP, death release, pool reuse resets multiplier/HP/IsDead/velocity, W10 1.45, all four EnemyData assets unchanged, regressions), 0 errors/warnings.
- [x] M8.3 Boss (Wave 10) (2026-08-16): `BossData : EnemyData` (minimal subclass; asset MaxHP 500 / Damage 20 / MoveSpeed 1.5 / Range 1.5 / Cooldown 1.0 — M8.3 implementation params, not GAME_DESIGN). `BossAI` (pursues player via MovePosition; CONTACT damage via OnTriggerEnter2D → CombatSystem DamageRequest Source=boss, Damage=Stats.Damage; Time.time cooldown = AttackCooldown; player-only target; no direct PlayerHealth.TakeDamage). `EnemySpawner.SpawnBoss(position, multiplier)` — same pool dictionary, no second pool. `WaveManager` Wave 10 = boss encounter: one boss spawned (W10 multiplier 1.45) instead of the normal schedule; BossSpawned published; boss defeat matched via EnemyKilled(Enemy == active boss) → BossDefeated → TryChangeState(Victory) (legal transition, GameManager untouched); no WaveCompleted(10) by time, no wave 11. Boss death still flows EnemyDied + EnemyKilled(Killer==Player) + PickupSystem (1 XP + 1 Gold). Boss prefab + BossData asset + SC_Main bossPrefab wiring. Verified: 47/47 probe checks PASS (BossData/prefab/components, W10 scaling MaxHP 725/Damage 29/MoveSpeed 2.175, Range/Cooldown unchanged, pursuit, contact damage via CombatSystem + player-only, death → EnemyKilled once + Release + pickups, manual kill outside Wave 10 no Victory, Wave 10 no normal spawns + BossSpawned once + no Victory while alive, BossDefeated once → Victory not re-triggered + wave stopped + no more spawns, pool reuse clean (IsDead/HP/velocity/multiplier/AI), regressions), 0 errors/warnings.
- [x] M8 Final Regression & Acceptance (2026-08-16): full flow MainMenu → Playing → W1 (natural run, WaveCompleted(1) once, W2 auto-start) → pause-freeze (Paused/LevelUp/Shop) → GameOver stop → restart at W1 → W10 boss encounter (BossSpawned once, no normal spawns, W10 multiplier 1.45) → boss contact damage via CombatSystem → BossDefeated once → Victory (no duplicate, wave stopped, no wave 11) → boss pickups + pool reuse clean → Pulse Gun kills test enemies + pickups. EventBus counts per design; cross-play static state clean (3 final Play/Stops, 0/0 each). Verified: 58/58 probe checks PASS, 0 errors/warnings.
- [ ] M6.5 Arc Blade

## Rules
- Only mark a task complete after actual verification.
- When tasks change, keep this document synchronized with the current project state.

## M3 Bug Fix #2 (2026-08-14) — "return to center" investigation
- [x] Full-project audit of all position writes (only CameraFollow→Camera, PlayerController→MovePosition; no reset code)
- [x] In-assembly runtime probe (temporary AutoProbe): W/A/D movement exact (5 u/s), position kept after release across 3 Play/Stop cycles
- [x] Camera follow verified (player ↔ camera aligned)
- [x] Root cause: empty scene = no visual reference; camera-follow movement invisible → perceived "small range + return to center" (world coordinates never reset)
- [x] Fix: added Ground reference (GroundPlaceholder grid, sortingOrder -10); no player/camera logic changed
- [x] Final clean play/stop: 0 errors / 0 warnings; temp probe removed

## M3 Full Review + Visual Scale (2026-08-14)
- [x] Full code review (Player 4 scripts + Core 6 scripts): clean, no temp/debug residue, no required fixes
- [x] Scene/Prefab review: no missing components, inputActions persisted, single Player, Rigidbody2D Interpolate
- [x] Temp residue scan: zero matches; all 10 scripts are formal
- [x] Regression 15/15 PASS (temporary probe, removed)
- [x] Visual scale: Camera size 7, Player scale 1.5, Ground 8px grid (4-unit spacing); bounds/speed unchanged
- [x] Final play/stop: 0 errors / 0 warnings

## Current Milestone: M9 — Roguelite / Upgrade
- [x] M9.1 XP Level Up (2026-08-16): `PlayerProgress` gained level state — Level (start 1), XPToNextLevel (PLACEHOLDER 100 × level, explicitly not a GAME_DESIGN value), AddXP accumulates with XP carry-over and multi-level support (one AddXP can cross several levels, loop guaranteed to terminate); publishes `PlayerLevelUp(level)` once per level gained (new GameEvents struct). `PlayerLevelSystem` (new, on Player scene + prefab) listens for PlayerLevelUp and — only while Playing — calls GameManager.TryChangeState(LevelUp) (legal transition, GameManager untouched); non-Playing states are never force-entered. WaveManager untouched: LevelUp freezes wave time/spawns, resume continues the same wave without re-publishing WaveStarted. No upgrade chooser/UI/stats modifiers (M9.2+). Verified: 41/41 probe checks PASS (initial Level 1/XP 0, threshold 100 placeholder, AddXP(0)/negative ignored, 90 XP no level-up + no event, 90+20 → Level 2 / carry 10 + PlayerLevelUp(2) once + threshold 200, Playing → LevelUp not re-entered, WaveElapsed/spawns frozen + wave not reset, resume continues + no duplicate WaveStarted, 340 → Level 3 / XP 150 + event, 1000 → Level 5 / XP 450 crossing levels with PlayerLevelUp(4)+(5) each once, GameOver XP processes but state not force-entered, Gold/PlayerHealth/weapons/Boss prefab/Boomerang regressions), 0 errors/warnings.
- [x] M9.2 Upgrade Chooser Logic (2026-08-16): `UpgradeData : ScriptableObject` (UpgradeId/DisplayName/StatType enum (10)/Amount, additive) + 10 assets (MaxHP +10 / HPRegen +0.5 / MoveSpeed +0.5 / Damage +1 / AttackSpeed +0.1 / CritChance +0.02 / CritDamage +0.25 / Range +0.5 / PickupRange +0.5 / Armor +1 — M9.2 implementation placeholders, NOT GAME_DESIGN balance). `PlayerStats` runtime bonus layer (per-stat private bonus fields; accessors = base + bonus; `ApplyUpgrade` adds to matching bonus; `ResetForRun` zeroes all; serialized base NEVER modified). `UpgradeManager` (on Player scene+prefab, pool of 10): pending level-up queue (each PlayerLevelUp → pending++; LevelUp only while Playing; no re-entry), `GenerateOptions` (3 UNIQUE random from pool, no weights; different level-ups may repeat), `SetForcedOptions` test hook, `Select(index)` (guards: waiting + LevelUp + valid index; applies chosen once via PlayerStats.ApplyUpgrade, publishes `UpgradeSelected(upgrade, level)` (new GameEvents struct), pending--; pending>0 → stays LevelUp + next options; pending==0 → TryChangeState(Playing)). WaveManager untouched (LevelUp freezes/resumes wave). Verified: 58/58 probe checks PASS (10 assets + one per stat, base stats, ApplyUpgrade MaxHP/MoveSpeed/Armor, ResetForRun, XP basics, 90 XP no level-up, Level 2 + PlayerLevelUp(2) once + pending 1 + LevelUp state, wave frozen + no duplicate WaveStarted, 3 options non-null+unique, Select applies chosen only + UpgradeSelected once + Playing + wave resumes, double/invalid/non-LevelUp select ignored, GameOver/MainMenu/Playing protection, 200 XP → Level 3 options again, one AddXP crossing Level 3→5 (pending 2, two selects, both applied, two UpgradeSelected, wave restored once), regressions), 0 errors/warnings.
- [x] M9.3 LevelUp UI Panel (2026-08-16): `UpgradeOptionsGenerated(option0..2)` fact event (new GameEvents struct) published by UpgradeManager AFTER Options is fully written (GenerateOptions + SetForcedOptions both publish). `LevelUpPanel` (new, UI namespace, component on the ACTIVE Canvas — inactive objects never Awake): GameStateChanged drives visibility (LevelUp → show; Playing/GameOver/Victory/MainMenu → hide; Paused untouched), UpgradeOptionsGenerated drives the 3 button labels (DisplayName / StatType / +Amount — integer amounts as integers, floats raw, no %), button.onClick.AddListener wired once → UpgradeManager.Select(i) (no stats/state writes). Scene SC_Main gains Canvas (ScreenSpaceOverlay + CanvasScaler 1920x1080 match 0.5 + GraphicRaycaster) + EventSystem with InputSystemUIInputModule (Unity Input System; no StandaloneInputModule) + LevelUpPanel hierarchy (Title LEVEL UP! + 3 buttons). Panel stays visible across consecutive pending level-ups (refreshes options #2) and hides only when the queue drains back to Playing. Verified: 55/55 probe checks PASS (Canvas/EventSystem/InputModule/Panel hidden initially, 3 buttons, title, LevelUp → panel visible + options event, 3 labels match DisplayName/StatType/Amount, REAL button.onClick.Invoke() applies chosen only + UpgradeSelected once + Playing + panel hidden + wave resumes, duplicate click ignored, consecutive L3→L5: panel stays visible, second set actually refreshed + applied, Playing + hidden after drain, non-LevelUp states hidden, exactly 1 Canvas/EventSystem/Panel (no duplicates), regressions), 0 errors/warnings.
- [x] M9.4 Shop (2026-08-16): `PlayerProgress.TrySpendGold(int)` (only gold-spending entry; <=0/insufficient rejected, never negative). `ShopItemData : ScriptableObject` (ShopItemType Weapon/StatBonus, DisplayName, Price, WeaponPrefab, Upgrade reference) + 14 assets (4 weapons at 30, 10 stat bonuses at 20 — M9.4 implementation prices, NOT GAME_DESIGN). `ShopManager` (new, scene object): listens WaveCompleted (W1..W9 only — Wave 10 never publishes it) → GenerateProducts (4 = 2 weapons + 2 stats, unique per shop, no weights) → TryChangeState(Shop) (legal, GameManager untouched); Purchase(i) — stat bonus via UpgradeData → PlayerStats.ApplyUpgrade; weapon via prefab Instantiate + WeaponManager.Equip into EMPTY slot only, gold spent AFTER successful equip, already-owned (same WeaponData) and no-empty-slot rejected without spending; SetForcedProducts test hook; Refresh (flat 20 gold, unlimited — placeholder rule) re-rolls + resets purchase state; Continue → Playing (WaveManager resumes the pre-started next wave). `ShopPanel` (UI, on active Canvas like LevelUpPanel): GameStateChanged show/hide, ShopProductsGenerated (new GameEvents struct) drives 4 product buttons (DisplayName/Type/Price) + gold text, Purchase/Refresh/Continue buttons. Single Canvas/EventSystem preserved. Verified: 58/58 probe checks PASS (gold spend rules, W1→Shop + wave frozen, 4 unique products 2/2, stat purchase +20 gold + apply + no double-buy, weapon purchase into empty slot +30 gold + equipped, already-owned PulseGun rejected, no-empty-slot rejected no-spend, refresh 20 + re-roll + reset, refresh insufficient-gold fail, Continue→Playing→wave resumes no duplicate WaveStarted, W10 boss→Victory no shop, UI buttons/Canvas/EventSystem single instance, regressions), 0 errors/warnings.
- [ ] M9.5 Weapon Upgrade (pending)
