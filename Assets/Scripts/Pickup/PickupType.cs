namespace VoidSurvivor.Pickup
{
    /// <summary>
    /// Pickup reward kinds (M5.3). Only XP and Gold exist in this milestone;
    /// heal/power-up/weapon/rarity kinds are out of scope.
    /// </summary>
    public enum PickupType
    {
        XP,
        Gold,
    }
}
